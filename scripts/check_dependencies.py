import torch
import pandas as pd
import transformers
import librosa
import fastapi
import streamlit
import langchain
import faiss
import pytest

print("All Day 3 dependencies imported successfully.")
print(f"PyTorch version: {torch.__version__}")
print(f"Pandas version: {pd.__version__}")
print(f"Transformers version: {transformers.__version__}")
